<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".EmptyAndroidApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EmptyAndroid"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.EmptyAndroid">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Demo：演示 core:base + core:viewbinding 框架用法 -->
        <activity
            android:name=".demo.TaskListActivity"
            android:exported="false"
            android:theme="@style/Theme.EmptyAndroid" />

        <!-- Demo：完整多类型 Adapter + MVI State/Effect 示例 -->
        <activity
            android:name=".demo.ProductCatalogActivity"
            android:exported="false"
            android:theme="@style/Theme.EmptyAndroid" />
    </application>

</manifest>
